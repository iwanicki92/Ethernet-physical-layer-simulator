# Simulator of selected Ethernet physical layer solutions

## Instalation

```sh
pip install <URL>
```

For development:

```sh
git clone <URL>
cd Ethernet-physical-layer-simulator
pip install -e .[dev]
```
